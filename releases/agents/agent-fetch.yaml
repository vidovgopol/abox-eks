apiVersion: kagent.dev/v1alpha2
kind: Agent
metadata:
  name: simple-fetch-agent
  namespace: kagent
spec:
  description: This agent can use a single tool to retrieve the contents of a webpage.
  type: Declarative
  declarative:
    modelConfig: default-model-config
    a2aConfig:
      skills:
        - id: web-fetch
          name: Web Fetch
          description: Retrieve and summarise the contents of any webpage
        - id: weather-lookup
          name: Weather Lookup
          description: Get current weather for any location
    systemMessage: |-
      You're a friendly and helpful agent that uses the fetch tool to retrieve webpage contents.

      # Instructions
      - If user question is unclear, ask for clarification before running any tools
      - Always be helpful and friendly
      - If you don't know how to answer the question DO NOT make things up
        respond with "Sorry, I don't know how to answer that" and ask the user to further clarify the question

      # Response format
      - ALWAYS format your response as Markdown
      - Your response will include a summary of actions you took and an explanation of the result
    tools:
    - type: McpServer
      mcpServer:
        apiGroup: kagent.dev
        kind: MCPServer
        name: mcp-website-fetcher
        toolNames:
        - fetch
    - type: McpServer
      mcpServer:
        apiGroup: kagent.dev
        kind: MCPServer
        name: test-kmcp-server
        toolNames:
        - echo
        - weather
