apiVersion: v1
kind: Service
metadata:
  name: support-agent
  namespace: kagent
spec:
  selector:
    app.kubernetes.io/name: support-agent
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
